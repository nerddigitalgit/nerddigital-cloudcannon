{
  "permissions": {
    "allow": [
      "Read",
      "Edit",
      "Write",
      "Glob",
      "Grep",
      "WebFetch",
      "WebSearch",
      "Bash(*)"
    ],
    "deny": [
      "Bash(rm -rf /)",
      "Bash(sudo rm -rf *)"
    ]
  },
  "commands": {
    "meeting": {
      "model": "haiku",
      "description": "Process meeting transcripts (uses Haiku for cost efficiency)"
    }
  }
}
