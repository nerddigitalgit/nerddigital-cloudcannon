{
  "name": "nerddigital-cloudcannon",
  "private": true,
  "scripts": {
    "postinstall": "cd websitebygemini/ndwebsite-gemini && npm install",
    "build": "cd websitebygemini/ndwebsite-gemini && npm run build && cp -r dist ../../dist"
  }
}
